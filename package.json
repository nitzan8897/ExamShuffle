{
  "name": "examshuffle",
  "version": "2.0.0",
  "private": true,
  "description": "Exam PDF in -> AI-shuffled exam PDF out (pixel-copied questions, RTL, answer key)",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev -w server\" \"npm run dev -w web\"",
    "build": "npm run build -w web",
    "start": "npm run start -w server",
    "cli": "tsx server/src/cli.ts",
    "sample": "tsx server/scripts/make-sample.ts",
    "typecheck": "npm run typecheck -w server && npm run typecheck -w web"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
